to run the python file
open the directory with gendered-abuse-detection-in-indic-languages.ipynb file , ensure dataset is in the same directory

to install the requirements
pip install -r requirements.txt

run all cells in the ipynb file